<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appsforgood">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RepCollage"></activity>
        <activity android:name=".Bills" />
        <activity android:name=".Representatives" />
        <activity android:name=".LearnActivity" />
        <activity android:name=".Correct" />
        <activity android:name=".QuizActivity1" /> <!-- <activity android:name=".QuizActivity1"></activity> -->
        <!-- android:name="com.example.appsforgood" -->
        <!-- <activity android:name=".QuizActivity"></activity> -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>